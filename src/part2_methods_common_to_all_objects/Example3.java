package part2_methods_common_to_all_objects;

//Always override toString

public class Example3 {

    //Include a java comment above toString() explaining how the string representation is generated
    //Try to include all of an object's interesting information when generating the string representation
    //Be careful with giving a specific format to the representation. (e.g- returning DD-MM-YYYY for a date class. cannot add any additional properties later)
    //Fields that goes into generating the representation should be able to access through accessors(getters)
}
